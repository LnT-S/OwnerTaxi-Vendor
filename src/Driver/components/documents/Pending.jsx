import React from 'react';
import {View, StyleSheet, Text} from 'react-native';
import DocumentCard from './CustomDocumentPicker';

const Pending = (props) => {
    return (
        <View>
            <Text>Pending</Text>
        </View>
    );
}

const styles = StyleSheet.create({})

export default Pending;
